---
phase: 07-auto-update-detection-fix
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - frontend/src/stores/updateStore.ts
  - frontend/src/components/AboutDialog.vue
  - frontend/src/components/UpdateDialog.vue
autonomous: false

must_haves:
  truths:
    - "用户点击检查更新按钮时显示加载状态"
    - "检查更新时显示 toast 提示（正在检查/检查完成/检查失败）"
    - "网络错误时显示清晰的错误消息和重试建议"
    - "API 速率限制时提供用户友好的提示"
    - "关于界面显示完整的版本信息卡片"
    - "版本信息卡片包含 GitHub Releases 下载链接按钮"
    - "版本信息卡片包含可折叠的更新说明(changelog)区域"
  artifacts:
    - path: "frontend/src/stores/updateStore.ts"
      exports: ["checkForUpdates", "formatUpdateError"]
    - path: "frontend/src/components/AboutDialog.vue"
      contains: "version-info-card|updateInfo|download-link|changelog"
    - path: "frontend/src/components/UpdateDialog.vue"
      contains: "formatUpdateError"
  key_links:
    - from: "frontend/src/stores/updateStore.ts"
      to: "frontend/src/stores/errorStore.ts"
      via: "import and useErrorStore"
      pattern: "useErrorStore|addError"
    - from: "frontend/src/components/AboutDialog.vue"
      to: "wailsjs/go/main/App"
      via: "CheckForUpdates"
      pattern: "await CheckForUpdates"
    - from: "frontend/src/components/AboutDialog.vue"
      to: "GitHub Releases"
      via: "download button"
      pattern: "releases/download"
---

<objective>
增强更新检测的用户反馈机制，在关于界面显示版本信息卡片，提供检查更新时的即时加载状态和 toast 提示，以及清晰友好的错误消息。

Purpose: 用户反馈点击"检查更新"按钮没有反馈，无法判断检测是否成功。关于界面看不到版本信息（当前版本、最新版本、更新状态）。通过增强 UI 反馈和错误提示，提升用户体验。

Output: 增强的版本信息展示和用户反馈机制。
</objective>

<execution_context>
@C:\Users\allan716\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\allan716\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-auto-update-detection-fix/07-CONTEXT.md
@.planning/phases/07-auto-update-detection-fix/07-RESEARCH.md
@.planning/phases/07-auto-update-detection-fix/07-01-SUMMARY.md
@frontend/src/stores/updateStore.ts
@frontend/src/stores/errorStore.ts
@frontend/src/components/AboutDialog.vue
@frontend/src/components/UpdateDialog.vue
@frontend/src/components/ErrorToast.vue
</context>

<tasks>

<task type="auto">
  <name>Task 1: 增强 updateStore 的错误处理和用户反馈</name>
  <files>frontend/src/stores/updateStore.ts</files>
  <action>
    修改 frontend/src/stores/updateStore.ts，增强 checkForUpdates 方法的用户反馈：

    1. 导入 useErrorStore
    2. 添加 formatUpdateError 工具函数
    3. 修改 checkForUpdates 方法添加 toast 提示

    在文件顶部添加 errorStore 导入：

    ```typescript
    import { useErrorStore } from './errorStore'
    ```

    在 checkForActions 函数中修改（约第 43-58 行）：

    ```typescript
  // Actions
  async function checkForUpdates() {
    const errorStore = useErrorStore()

    isChecking.value = true

    // 显示即时反馈
    await errorStore.addError(
      '正在检查更新...',
      undefined,
      'warning',
      'UpdateService'
    )

    try {
      // 这里调用后端 API
      const { CheckForUpdates } = await import('../../wailsjs/go/main/App')
      const info = await CheckForUpdates()
      updateInfo.value = info
      hasUpdate.value = info.hasUpdate

      // 成功提示（2秒后自动消失）
      if (info.hasUpdate) {
        await errorStore.addError(
          `发现新版本 ${info.latestVersion}`,
          `当前版本: ${info.currentVersion}`,
          'warning',
          'UpdateService'
        )
      } else {
        await errorStore.addError(
          '当前已是最新版本',
          `最新版本: ${info.latestVersion}`,
          'warning',
          'UpdateService'
        )
      }

      return info
    } catch (error) {
      console.error('检查更新失败:', error)

      // 详细错误提示（显示 5 秒）
      const errorMessage = formatUpdateError(error)
      await errorStore.addError(
        `检查更新失败: ${errorMessage}`,
        error instanceof Error ? error.message : String(error),
        'error',
        'UpdateService'
      )

      throw error
    } finally {
      isChecking.value = false
    }
  }

  // 格式化错误消息（用户友好）
  function formatUpdateError(error: unknown): string {
    const err = error as { code?: string; message?: string }

    if (err.message?.includes('403')) {
      return 'GitHub API 速率限制，请稍后再试或稍后手动检查'
    }

    if (err.message?.includes('timeout') || err.message?.includes('deadline exceeded')) {
      return '网络连接超时，请检查网络设置'
    }

    if (err.message?.includes('failed to fetch') || err.message?.includes('connection refused')) {
      return '无法连接到 GitHub，请检查网络连接'
    }

    if (err.message?.includes('Atom feed')) {
      return 'GitHub 版本信息获取失败，请稍后重试'
    }

    return err.message || '未知错误，请稍后重试'
  }
    ```

    4. 在 return 语句中添加 formatUpdateError 导出：

    ```typescript
  return {
    // ... 现有导出
    formatUpdateError,
    formatBytes,
    formatSpeed,
    calculateETA
  }
    ```
  </action>
  <verify>cd frontend && npm run type-check 2>&1 | head -20</verify>
  <done>checkForUpdates 方法添加即时反馈和错误处理，formatUpdateError 函数导出</done>
</task>

<task type="auto">
  <name>Task 2: 在 AboutDialog 添加完整版本信息卡片（含下载链接和更新说明）</name>
  <files>frontend/src/components/AboutDialog.vue</files>
  <action>
    修改 frontend/src/components/AboutDialog.vue，在版本信息区域添加完整的版本信息卡片，必须包含：
    1. GitHub Releases 下载链接按钮
    2. 可折叠的更新说明(changelog)区域

    修改 script setup 部分（约第 66-112 行）：

    ```typescript
    <script setup lang="ts">
    import { ref, onMounted, watch, computed } from 'vue'
    import { GetVersion, GetFullVersion, CheckForUpdates } from '../../wailsjs/go/main/App'
    import { useUpdateStore } from '../stores/updateStore'
    import { useErrorStore } from '../stores/errorStore'

    const props = defineProps<{
      visible: boolean
    }>()

    const emit = defineEmits(['close'])

    const updateStore = useUpdateStore()
    const errorStore = useErrorStore()
    const version = ref('加载中...')
    const fullVersion = ref('')
    const isCheckingUpdate = ref(false)
    const isChangelogExpanded = ref(false)
    const updateInfo = ref<{
      latestVersion: string
      currentVersion: string
      hasUpdate: boolean
      publishedAt: string
      releaseNotes: string
      isPrerelease: boolean
      downloadUrl: string
    } | null>(null)

    // GitHub 仓库信息（用于构建下载链接）
    const GITHUB_REPO = 'allanpk716/ai-commit-hub'

    // 计算下载链接
    const downloadUrl = computed(() => {
      if (updateInfo.value && updateInfo.value.latestVersion) {
        return `https://github.com/${GITHUB_REPO}/releases/download/${updateInfo.value.latestVersion}/`
      }
      return `https://github.com/${GITHUB_REPO}/releases/latest`
    })

    // 加载版本信息
    async function loadVersion() {
      try {
        version.value = await GetVersion()
        fullVersion.value = await GetFullVersion()
      } catch (error) {
        console.error('获取版本信息失败:', error)
        version.value = '未知版本'
        fullVersion.value = ''
      }
    }

    // 检查更新
    async function checkForUpdates() {
      if (isCheckingUpdate.value) {
        return // 防止重复点击
      }

      isCheckingUpdate.value = true

      try {
        await errorStore.addError(
          '正在检查更新...',
          undefined,
          'warning',
          'AboutDialog'
        )

        const info = await CheckForUpdates()
        updateInfo.value = {
          latestVersion: info.latestVersion,
          currentVersion: info.currentVersion,
          hasUpdate: info.hasUpdate,
          publishedAt: info.publishedAt ? new Date(info.publishedAt).toLocaleDateString('zh-CN', {
            year: 'numeric',
            month: 'long',
            day: 'numeric'
          }) : '',
          releaseNotes: info.releaseNotes || '',
          isPrerelease: info.isPrerelease,
          downloadUrl: `https://github.com/${GITHUB_REPO}/releases/download/${info.latestVersion}/`
        }

        // 如果有更新，自动展开更新说明
        if (info.hasUpdate && info.releaseNotes) {
          isChangelogExpanded.value = true
        }

        if (info.hasUpdate) {
          await errorStore.addError(
            `发现新版本 ${info.latestVersion}`,
            `当前版本: ${info.currentVersion}`,
            'warning',
            'AboutDialog'
          )
        } else {
          await errorStore.addError(
            '当前已是最新版本',
            `最新版本: ${info.latestVersion}`,
            'warning',
            'AboutDialog'
          )
        }
      } catch (error) {
        console.error('检查更新失败:', error)
        await errorStore.addError(
          '检查更新失败',
          error instanceof Error ? error.message : String(error),
          'error',
          'AboutDialog'
        )
      } finally {
        isCheckingUpdate.value = false
      }
    }

    // 打开下载链接
    function openDownloadPage() {
      const url = downloadUrl.value
      window.open(url, '_blank')
    }

    // 切换更新说明展开状态
    function toggleChangelog() {
      isChangelogExpanded.value = !isChangelogExpanded.value
    }

    // 关闭对话框
    function close() {
      emit('close')
    }

    // 格式化预发布版本显示
    function formatPrereleaseBadge(isPrerelease: boolean, version: string) {
      if (!isPrerelease) return ''
      if (version.includes('alpha')) return ' (Alpha)'
      if (version.includes('beta')) return ' (Beta)'
      if (version.includes('rc')) return ' (RC)'
      return ' (预发布)'
    }

    // 监听对话框打开，自动检查更新
    watch(() => props.visible, async (isVisible) => {
      if (isVisible) {
        await loadVersion()
        await checkForUpdates()
      } else {
        // 关闭时重置状态
        isChangelogExpanded.value = false
      }
    })

    // 组件挂载时加载版本
    onMounted(async () => {
      if (props.visible) {
        await loadVersion()
        await checkForUpdates()
      }
    })
    </script>
    ```

    修改 template 部分，在版本信息区域添加完整信息（约第 17-28 行替换）：

    ```vue
        <!-- 版本信息卡片 -->
        <div class="version-info-card">
          <div class="version-info">
            <div class="version-row">
              <span class="label">当前版本:</span>
              <span class="value">{{ version }}</span>
            </div>

            <div v-if="updateInfo" class="version-row">
              <span class="label">最新版本:</span>
              <span class="value">
                {{ updateInfo.latestVersion }}
                <span v-if="updateInfo.isPrerelease" class="prerelease-badge">
                  {{ formatPrereleaseBadge(updateInfo.isPrerelease, updateInfo.latestVersion) }}
                </span>
              </span>
            </div>

            <div v-if="updateInfo" class="version-row">
              <span class="label">更新状态:</span>
              <span
                :class="['value', 'status-badge', updateInfo.hasUpdate ? 'has-update' : 'latest']"
              >
                {{ updateInfo.hasUpdate ? '有新版本可用' : '已是最新版本' }}
              </span>
            </div>

            <div v-if="updateInfo && updateInfo.publishedAt" class="version-row details">
              <span class="label">发布时间:</span>
              <span class="value">{{ updateInfo.publishedAt }}</span>
            </div>

            <div v-if="fullVersion !== version" class="version-row details">
              <span class="label">完整信息:</span>
              <span class="value monospace">{{ fullVersion }}</span>
            </div>
          </div>

          <!-- 下载链接按钮 -->
          <div v-if="updateInfo" class="version-actions">
            <a
              :href="downloadUrl"
              target="_blank"
              class="download-link-btn"
              title="打开 GitHub Releases 页面下载"
            >
              <span class="icon">📥</span>
              <span>{{ updateInfo.hasUpdate ? '下载新版本' : '访问下载页面' }}</span>
              <span class="external-icon">↗</span>
            </a>
          </div>

          <!-- 可折叠的更新说明 -->
          <div v-if="updateInfo && updateInfo.releaseNotes" class="changelog-section">
            <button
              @click="toggleChangelog"
              class="changelog-toggle"
              :class="{ expanded: isChangelogExpanded }"
            >
              <span class="icon">{{ isChangelogExpanded ? '▼' : '▶' }}</span>
              <span>更新说明</span>
              <span v-if="updateInfo.hasUpdate" class="new-badge">新</span>
            </button>
            <div v-show="isChangelogExpanded" class="changelog-content">
              <div class="changelog-text" v-html="formatReleaseNotes(updateInfo.releaseNotes)"></div>
            </div>
          </div>
        </div>
    ```

    添加 formatReleaseNotes 辅助函数到 script 部分：

    ```typescript
    // 格式化更新说明（保留换行和链接）
    function formatReleaseNotes(notes: string): string {
      if (!notes) return ''
      // 转换换行符为 <br>
      let formatted = notes.replace(/\n/g, '<br>')
      // 转换 Markdown 风格的链接 [text](url) 为 HTML 链接
      formatted = formatted.replace(/\[([^\]]+)\]\(([^)]+)\)/g, '<a href="$2" target="_blank" class="release-link">$1</a>')
      return formatted
    }
    ```

    修改检查更新按钮，添加加载状态（约第 56-59 行）：

    ```vue
        <button
          @click="checkForUpdates"
          class="btn btn-secondary"
          :disabled="isCheckingUpdate"
        >
          <span class="icon" :class="{ spinning: isCheckingUpdate }">🔄</span>
          <span>{{ isCheckingUpdate ? '检查中...' : '检查更新' }}</span>
        </button>
    ```

    添加/更新样式（在 style 部分末尾添加）：

    ```css
    /* 版本信息卡片容器 */
    .version-info-card {
      background: #f9fafb;
      border-radius: 8px;
      padding: 16px;
      margin-bottom: 24px;
      border: 1px solid #e5e7eb;
    }

    .prerelease-badge {
      font-size: 11px;
      color: var(--accent-warning);
      font-weight: normal;
    }

    .status-badge {
      padding: 4px 12px;
      border-radius: 12px;
      font-size: 12px;
      font-weight: 600;
    }

    .status-badge.has-update {
      background: #fef3c7;
      color: #92400e;
    }

    .status-badge.latest {
      background: #d1fae5;
      color: #065f46;
    }

    /* 下载链接按钮 */
    .version-actions {
      margin-top: 12px;
      padding-top: 12px;
      border-top: 1px solid #e5e7eb;
    }

    .download-link-btn {
      display: inline-flex;
      align-items: center;
      gap: 8px;
      padding: 10px 16px;
      background: linear-gradient(135deg, #3b82f6 0%, #2563eb 100%);
      color: white;
      border-radius: 8px;
      text-decoration: none;
      font-size: 14px;
      font-weight: 500;
      transition: all 0.2s;
      box-shadow: 0 2px 4px rgba(59, 130, 246, 0.2);
    }

    .download-link-btn:hover {
      background: linear-gradient(135deg, #2563eb 0%, #1d4ed8 100%);
      box-shadow: 0 4px 8px rgba(59, 130, 246, 0.3);
      transform: translateY(-1px);
    }

    .download-link-btn .external-icon {
      font-size: 12px;
      opacity: 0.7;
    }

    /* 更新说明区域 */
    .changelog-section {
      margin-top: 12px;
      padding-top: 12px;
      border-top: 1px solid #e5e7eb;
    }

    .changelog-toggle {
      display: flex;
      align-items: center;
      gap: 8px;
      width: 100%;
      padding: 8px 12px;
      background: white;
      border: 1px solid #d1d5db;
      border-radius: 6px;
      cursor: pointer;
      font-size: 14px;
      font-weight: 500;
      color: #374151;
      transition: all 0.2s;
    }

    .changelog-toggle:hover {
      background: #f9fafb;
      border-color: #9ca3af;
    }

    .changelog-toggle .icon {
      font-size: 10px;
      transition: transform 0.2s;
    }

    .changelog-toggle.expanded .icon {
      transform: rotate(90deg);
    }

    .new-badge {
      margin-left: auto;
      background: #ef4444;
      color: white;
      font-size: 10px;
      padding: 2px 6px;
      border-radius: 10px;
      font-weight: 600;
    }

    .changelog-content {
      margin-top: 8px;
      padding: 12px;
      background: white;
      border-radius: 6px;
      border: 1px solid #e5e7eb;
    }

    .changelog-text {
      font-size: 13px;
      line-height: 1.6;
      color: #4b5563;
      max-height: 200px;
      overflow-y: auto;
    }

    .changelog-text :deep(.release-link) {
      color: #3b82f6;
      text-decoration: underline;
    }

    .changelog-text :deep(.release-link:hover) {
      color: #2563eb;
    }

    .icon.spinning {
      animation: spin 1s linear infinite;
    }

    @keyframes spin {
      from { transform: rotate(0deg); }
      to { transform: rotate(360deg); }
    }
    ```
  </action>
  <verify>cd frontend && npm run type-check 2>&1 | head -20</verify>
  <done>AboutDialog 显示完整版本信息卡片，包含下载链接按钮和可折叠的更新说明</done>
</task>

<task type="checkpoint:human-verify">
  <what-built>
    1. updateStore 增强了用户反馈（toast 提示和错误处理）
    2. AboutDialog 添加了完整的版本信息卡片，包含：
       - 当前版本、最新版本、更新状态、发布时间
       - GitHub Releases 下载链接按钮
       - 可折叠的更新说明(changelog)区域
    3. 检查更新按钮显示加载状态
  </what-built>
  <how-to-verify>
    手动测试验证：
    1. 启动应用：wails dev
    2. 打开关于对话框（点击工具栏的"关于"按钮）
    3. 验证版本信息卡片显示正确：
       - 当前版本号显示
       - 最新版本号显示
       - 更新状态徽章（已是最新/有新版本）
       - 发布时间显示（如果有）
    4. 验证下载链接按钮：
       - 按钮可见且样式正确（蓝色渐变背景）
       - 点击后打开 GitHub Releases 下载页面
       - 按钮文字根据状态变化（"下载新版本" / "访问下载页面"）
    5. 验证更新说明区域：
       - 点击"更新说明"按钮可展开/折叠
       - 展开时显示格式化的 release notes
       - 有新版本时自动展开并显示"新"徽章
    6. 点击"检查更新"按钮：
       - 按钮显示"检查中..."状态
       - 图标旋转动画
       - 右下角显示 toast 提示
    7. 测试网络错误场景（可选，断网后测试）：
       - 检查更新应显示友好的错误消息
  </how-to-verify>
  <resume-signal>确认版本信息、下载链接、更新说明都正常显示后回复 "approved" 或描述问题</resume-signal>
</task>

</tasks>

<verification>
1. TypeScript 类型检查：npm run type-check 通过
2. 编译验证：wails dev 启动成功
3. 功能验证：
   - 版本信息卡片显示完整
   - 下载链接按钮可见且可点击
   - 更新说明区域可展开/折叠
   - 检查更新按钮有加载状态
   - Toast 提示正常显示
   - 错误消息友好清晰
</verification>

<success_criteria>
1. 版本信息卡片在关于界面正确显示
2. GitHub Releases 下载链接按钮可见且功能正常
3. 更新说明(changelog)区域可展开/折叠，内容格式化显示
4. 检查更新按钮显示加载状态（旋转图标 + 文字变化）
5. Toast 提示在检查更新时显示（正在检查/检查完成）
6. 错误消息用户友好（速率限制/网络超时/连接失败）
7. 代码通过 TypeScript 类型检查
</success_criteria>

<output>
After completion, create `.planning/phases/07-auto-update-detection-fix/07-02-SUMMARY.md`
</output>
